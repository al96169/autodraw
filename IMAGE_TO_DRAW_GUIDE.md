# 从图片到 AutoDraw 绘制：完整工作流

将一张普通图片转化为 AutoDraw 可绘制的 SVG 线稿，分三步：选图 → AI 转简笔画 → 矢量描摹 → 导入绘制。

```
原始图片 ──→ 豆包AI转简笔画 ──→ Illustrator/Inkscape描摹为SVG ──→ AutoDraw绘制
```

## 第一步：选择适合的图片

适合转化为简笔画的图片特征：

- **主体清晰** — 单一主体居中，背景简洁
- **轮廓分明** — 物体与背景对比度高，边缘清晰
- **细节适中** — 线条不过于密集，避免复杂纹理
- **正面/侧面视角** — 轮廓特征明显，避免透视压缩

适合的例子：卡通角色、动物侧面照、花卉、建筑剪影、Logo
不适合的例子：人群合影、风景照、油画、写实人像

> 如果原图背景复杂，先用豆包「删除背景」或抠图工具处理成纯白背景。

## 第二步：用豆包 AI 转为简笔画

### 操作方法

1. 打开 [豆包](https://www.doubao.com)，上传图片
2. 使用图片编辑功能，输入风格转换指令

### 推荐提示词

根据原图类型选择：

| 图片类型 | 提示词 |
|---------|--------|
| 动物/角色 | 「改成简笔画风格，黑色线条，白色背景，线条简洁流畅，保留主要轮廓特征」 |
| 花卉/植物 | 「改成白描线条画，黑色细线，白色背景，只保留轮廓和主要叶脉」 |
| 建筑/物体 | 「改成线稿风格，黑色线条，白色背景，保留结构轮廓，去除阴影和纹理」 |
| 通用 | 「改成黑白简笔画，纯白背景，黑色连续线条，不填充色块，线条粗细均匀」 |

### 豆包提示词要点

- 每次只用一个核心指令，不要同时要求多种修改
- 强调「白色背景」「黑色线条」「不填充色块」，确保输出干净的线稿
- 强调「连续线条」「线条简洁」，减少断线和碎片
- 如果效果不理想，调整编辑强度（scale）重试

### 替代工具

如果豆包效果不理想，也可尝试：

- **即梦 AI**（字节跳动）— 同类图生图能力
- **Stable Diffusion**（本地部署）— 使用 ControlNet Lineart 模型
- **在线线稿转换** — 搜索「图片转线稿 在线工具」，如 [SPAI 线稿助理](https://www.spingseo.com/zh/line-art-edit/ai-sketch-converter)

## 第三步：用矢量工具描摹为 SVG

### 方案 A：Adobe Illustrator（推荐）

1. **导入线稿** — 将豆包生成的简笔画图片拖入 AI
2. **图像描摹** — 选中图片，点击菜单「对象 → 图像描摹 → 制作」
3. **调整参数** — 打开「窗口 → 图像描摹」面板：

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| 预设 | 技术绘图 | 适合线条图 |
| 模式 | 黑白 | 仅保留黑线 |
| 阈值 | 120-180 | 根据预览调整，确保线条完整 |
| 路径 | 80-90% | 越高越精细，但节点更多 |
| 边角 | 75-85% | 保持转角清晰 |
| 杂色 | 1-10px | 过滤细小噪点 |

4. **扩展** — 描摹满意后，点击「对象 → 扩展外观」，将描摹结果转为可编辑路径
5. **清理** — 删除白色背景填色区域，只保留黑色线条路径
6. **简化路径** — 选中所有路径，「对象 → 路径 → 简化」，减少节点数（曲线精度 90-95%）
7. **导出 SVG** — 「文件 → 导出 → 导出为 → SVG」，设置：
   - CSS 属性：演示文稿属性
   - 小数位数：2
   - 勾选「响应式」取消（固定尺寸）

### 方案 B：Inkscape（免费替代）

1. **导入线稿** — 将图片拖入 [Inkscape](https://inkscape.org/)
2. **白描位图** — 选中图片，按 `Shift+Alt+B` 打开白描面板
3. **选择模式**：
   - **亮度截断** — 适合高对比度黑白线稿，阈值 0.4-0.6
   - **边缘检测** — 适合边缘清晰的图，阈值 0.4-0.6
   - **中心线白描**（自动白描下）— **推荐**，生成中心线而非轮廓，最适合线稿
4. **简化路径** — 选中生成的路径，按 `Ctrl+L` 减少节点
5. **清理** — 删除原始位图和不需要的路径
6. **保存** — 「文件 → 另存为 → SVG (*.svg)」

> Inkscape 的「中心线白描」会生成真正的线条中心路径，比 Illustrator 的轮廓描摹更适合 AutoDraw 绘制，因为 AutoDraw 沿路径中心线走笔。

## 第四步：导入 AutoDraw 绘制

### 使用自带的示例 SVG

项目 `test/` 文件夹中已附带示例 SVG 文件，可直接拖入 AutoDraw 体验绘制效果，无需自行制作：

| 文件 | 内容 |
|------|------|
| `奶茶简笔画.svg` | 奶茶杯简笔画 |
| `乌萨奇*.svg` | 乌萨奇角色线稿（多个版本） |
| `吉伊*_画板 1.svg` | 吉伊角色线稿（多个版本） |
| `奶龙*_画板 1.svg` | 奶龙角色线稿（多个版本） |

> 这些 SVG 均已通过豆包 AI 转简笔画 + Illustrator/Inkscape 描摹制作，可作为制作参考。

### 绘制步骤

1. **启动 AutoDraw**，框选目标画布区域
2. **拖入 SVG** — 直接将 SVG 文件拖到 AutoDraw 画布上（可先用 `test/` 中的示例试试）
3. **调整属性** — 在右侧面板修改缩放比例和位置，使预览与画布对齐
4. **执行绘制** — 点击「执行绘制」，ESC 中止

### SVG 优化建议

为了让 AutoDraw 绘制效果更好，SVG 应尽量满足：

- **纯线条** — 不要有填充色块（AutoDraw 会自动过滤白色填充，但其他颜色填充会被绘制）
- **路径简洁** — 节点越少越流畅，使用 Illustrator/Inkscape 的简化功能
- **避免叠加** — 多条路径重叠会导致反复描画同一区域
- **尺寸适中** — SVG 文件不超过 40KB，路径不超过 1000 条，否则可能卡顿
- **viewBox 正确** — 确保导出的 SVG 包含正确的 `viewBox` 属性

> 如果 SVG 过大或路径过多，在 Illustrator 中用「对象 → 路径 → 简化」进一步减少节点，或手动删除不重要的细节路径。

## 完整流程示例

以画一只猫为例：

1. **选图** — 找一张猫咪侧面照，背景简单
2. **豆包转简笔画** — 上传图片，输入「改成简笔画风格，黑色线条，白色背景，线条简洁流畅，保留猫的轮廓特征」
3. **下载线稿** — 保存豆包生成的简笔画图片
4. **Inkscape 描摹** — 导入线稿，`Shift+Alt+B` 选择「中心线白描」，`Ctrl+L` 简化，保存为 `cat.svg`
5. **AutoDraw 绘制** — 拖入 `cat.svg`，调整大小，点「执行绘制」

## 参考

- [豆包图片编辑 Prompt 指南](https://blog.csdn.net/hsyq0755/article/details/150956252)
- [Adobe Illustrator 图像描摹预设](https://helpx.adobe.com/illustrator/using/image-trace-presets.html)
- [Inkscape 白描位图教程](https://inkscape.org/zh-hans/doc/tutorials/tracing/tutorial-tracing.html)
- [Inkscape 下载](https://inkscape.org/)
